# わんさかんさい（wansakansai.com）

## プロジェクト概要
関西エリアの犬連れお出かけスポットを現在地から近い順に探せる静的Webサイト。

- **読み**: わんさかんさい
- **ホスティング**: GitHub Pages（wansakansai.com）
- **ターゲット**: 関西在住・関西旅行中の犬の飼い主

## 変更履歴について（肥大化防止・2026-07-09導入）

時系列の履歴・完了済み施策の実装詳細・古い進捗語りは **CLAUDE_history.md**（.gitignore対象＝公開リポジトリに載せない）へ退避する。過去の経緯を調べる時だけ読むこと。**本体（このファイル）には現在形のルール・鉄則・決定事項・パス/ID・現在の状態のみを書き、以後の履歴行の追記もCLAUDE_history.mdへ**（このファイルには書かない）。

**★圧縮手順（scripts/check_claude_md_size.py が閾値超を通知した時。どのセッションでも「圧縮して」の一言で以下を実行）★**：
①CLAUDE.mdを圧縮前バックアップ（Dropbox: Claude_backup/自動タスク/wansakansai-claude-md/）②時系列履歴・完了済み施策の詳細・古い進捗語りをCLAUDE_history.mdへ退避（**ルール・鉄則・決定事項・パス/ID・現在の状態は残す**。削除ではなく移動）③別エージェントに圧縮前後＋アーカイブの全数比較をさせ**欠損ゼロを確認**（毎回参照が必要な運用ルールがアーカイブに埋まっていたら本体へ戻す）④両ファイルをDropboxへバックアップ。★無人タスクにはやらせない（対話セッション専用作業。自動タスクは検知と通知のみ）★

## 技術構成
- HTML + CSS + Vanilla JS（フレームワーク不使用）
- スポットデータ: JSON管理（data/spots.json）
- 地図: Google Maps iframe埋め込み（APIキー不要）
- お問い合わせ: Googleフォーム埋め込み
- ナビゲーション: common.jsのSITE_NAV定数で一元管理（ページ追加時は1行追加）。ヘッダーは主要4項目のみ（お気に入り/ブログ/このサイトについて/お問い合わせ）。トップ/テーマ別で探す/危険情報は `footerOnly: true` でフッターのみ掲載し、トップページ上部の「現在地から探す」「テーマ別で探す」ボタン＋危険情報バナーで導線を確保（ロゴ＝トップ）
- 都道府県フィルター: spots.jsonの住所から自動抽出（ハードコードなし）
- 個別対応は避け、テンプレート化・データ駆動を基本方針とする

## SEO・収益化
- Google Analytics設置済み（G-NPGCWSCZGB）
- Google Search Console登録済み（metaタグ認証 + HTMLファイル認証）
  - プロパティ: https://wansakansai.com/（2026-04-08追加、HTMLファイル認証）
- sitemap.xml / robots.txt 設置済み
- sitemap.xmlのベースURLはwansakansai.com（generate_sitemap.pyのBASE_URL）
- OGPタグは各HTMLの `<head>` に静的記述で統一（画像: images/ogp.png）。**JSでの動的注入は禁止**。OGPを追加・変更する時は静的HTMLに直接書く（spots/はgenerate_spot_pages.pyが埋め込む）
- **トップページのタイトルと説明文（2026-09-25見直し・ClaudeとCodexが別々に案を出して突き合わせ、Codex2巡目で支持）**: タイトル「関西で犬とお出かけ｜270か所以上から探せる｜わんさかんさい」。表示回数の最も多い検索語「犬とお出かけ 関西」（直近3か月3,430回・CTR 2%・9位前後）を先頭に置き、上位の「14選」「16選」のまとめ記事との違いとして件数を出した。**件数は「270か所以上」と下限で書く**（増えても削除が数件あっても嘘にならない）。**スポットが270を下回ると `generate_spot_pages.py` が★で警告する**ので、その時は index.html の title・description・og・構造化データを直す。300を超えたら数字を上げてよい。「穴場」「おすすめ」は根拠が無いので入れない。訪問済みは一部だけなので「実際に訪問」もトップの説明に入れない。経緯と両案は `Documents/wansakansai/research/top_title_2026-09-25/`
- canonical URL: 全ページに設定済み（spot.htmlはJSで動的更新）
- JSON-LD構造化データ: index.htmlにWebSite、spot.jsでPlace（スポットごとに動的生成）
- **AdSense**: 2026-05-25に adsbygoogle.js と ad-slot を全HTMLから除去（ads.txt は残置・本番で配信中）
  - **★2回とも不承認理由は「有用性の低いコンテンツ」（2026-04-19と2026-09-12・いずれも管理画面で実物確認）★** 技術面でなく**コンテンツの価値**の問題。経緯はCLAUDE_history.md。次に申請する時の操作＝管理画面→サイト→wansakansai.com→「問題を修正しました」→「審査をリクエスト」。**AdSenseのアカウントは ohsoccer3939@gmail.com（authuser=0）・pub-2097489177716087**。管理画面のURLは pub ID を含む形（`adsense.google.com/adsense/u/0/pub-2097489177716087/sites`）でないと「アクセスが拒否されました」になる
  - **承認されたら広告コードを戻す**（戻すまで広告は出ない）。具体的なタグ・設置場所はCLAUDE_history.md「広告コードの復元手順」
  - **★対策（2026-09-12・Claude と Codex が独立に集めた合格事例20件から）★** 通った人が最後に変えていたのは一次情報（自分の訪問・自前写真・訪問メモ）で、文字数・基本ページ・デザイン・PV・noindex単独は「やっても通らなかった」側（noindexのみで通った例は0/20）。わんさかんさいは前回の対策で後者を全部やり終えており、**テキストをこれ以上磨いても3回目も同じ理由で落ちる**。対策の本文は `C:/Users/imao_/Documents/wansakansai/adsense_recovery_plan_2026-09-12.md`（Dropbox: Claude_backup/自動タスク/wansakansai-claude-md/adsense_research_2026-09-12/ に Codex の原文と一緒に退避）
    - A 一次情報: 訪問して写真3〜5枚＋**訪問メモ（`visit`・未実装）**＋**写真キャプション（`captions`・未実装）**。外出1回につきブログ1本
    - B 編集されたメディアとして見せる: 「掲載情報について」ページ、**掲載情報の最終確認日（`lastChecked`・未実装）**、運営者情報を厚く
    - C 減らす: **新規スポットは「検索需要の根拠があり、訪問予定のあるもの」だけ追加する**（2026-09-12・ユーザー方針）。根拠は Search Console のクエリと Google サジェスト（`scripts/collect_suggest.py`・1,209語）。**時期別の重点リスト**は `C:/Users/imao_/Documents/wansakansai/research/seasonal_targets_2026-09-12.md`（秋→冬→春→夏の順。新規は2AI検証してから載せ、作ってから2〜3週間以内に撮りに行く）
    - D **★再申請は次が全部揃うまで押さない★**: 写真あり30件以上（13件から）・訪問済み全件に訪問メモ・ブログ6本以上（3本から）・掲載情報ページと最終確認日を公開・検索上位10ページの半分以上に写真。目安1〜2か月後。何も変えずに再申請して落ちた例が多数ある
  - CMP（EEA向け同意メッセージ）は未設定。訪問者はほぼ国内なので実害はほぼないが、設定は法令に関わるのでユーザー判断
- **アフィリエイト**: ①スポット詳細の**備考の直後**（`js/spot.js`・2026-09-10に最下部から移動）②ブログ記事の本文中と末尾（静的HTML・泊まった宿の施設ページへ**楽天・じゃらんとも直リンク**。じゃらんは `a8ejpredirect` 方式、2026-09-10）。楽天トラベル＋じゃらん/A8。`rel="sponsored"` 必須
  - **★枠を増やす前に、押される形に直す★**（2026-09-10の実測から。経緯はCLAUDE_history.md）。`affiliate_click` はGA4の**キーイベントに設定済み**
  - **スポット詳細の備考直後の枠は、スポットの性格で出し分ける**（`js/spot.js`）: ①`rakutenHotelId`（任意項目）… その宿泊施設の楽天トラベル施設ページへ直リンク（`jalanYadId` があればじゃらんも並べる） ②`stayKeyword`（任意項目）… 近隣の地名で「ペットと泊まれる宿」を検索（`kw.travel.rakuten.co.jp/keyword/Event.do?f_query=…&f_category=3&f_area=9`） ③**どちらも無ければ「犬とのお出かけに持っていくもの」の物販枠**（楽天市場・定番3点：リッチェル お散歩ハンディシャワー／ライオン ペットキレイ 除菌ウェットティッシュ／BOS うんちが臭わない袋。`data-aff="rakuten-item"`）。県別の宿リンクは廃止した（押されていなかった）。**商品を差し替える時は楽天の商品ページを開いて販売中を確認し、価格は書かない**（変動する）
    - **★楽天のキーワード検索は `f_query` が Shift_JIS★** UTF-8で渡すと文字化けして0件になる。JSにエンコーダが無いので、採用した地名だけ `STAY_KW_SJIS` の表で持つ（白浜・和歌山・長浜・琵琶湖・丹波）。**地名を足す時は必ず件数を確かめ、0件・1件のものは入れない**。`stayKeyword` は住所ルール（白浜町→白浜、長浜市→長浜、京丹波町/丹波篠山市/丹波市→丹波、和歌山市→和歌山）＋個別指定（琵琶湖）で26件に付与
    - `rakutenHotelId` は5件（かばたリゾート143245 / ホリデーアフタヌーン13999 / 四季の郷 遊楽145348 / パートナーズハウスゆあさ106158 / DOG HOTEL SHIRAHAMA193085）。**施設ページを開いて住所とペット宿泊情報を確認済み**。ホテルビワドッグ・みとろの丘は楽天に無い
    - **★じゃらんも施設ページへ直リンクできる★** A8の自由URL `https://px.a8.net/svt/ejp?a8mat=…&a8ejpredirect={URLエンコードした着地先}` で、指定ページに A8 の計測パラメータ付きで着地する。**着地先がトップに飛んだら、そのプログラムは自由URLを許可していない**（許可の有無はこれで分かる）。報酬は予約金額（税別）の1%。実証の経緯はCLAUDE_history.md。`jalanYadId`（任意項目・`jalan.net/yad{ID}/` のID）を持つスポットは「じゃらんで見る」を楽天の隣に出し、`data-aff="jalan-hotel"`。5件（かばた321469 / ホリデーアフタヌーン315849 / パートナーズハウス329512 / DOG HOTEL 340664 / 四季の郷338615）。**楽天が施設直リンクでじゃらんにIDが無い時だけ、じゃらんを出さない**（文脈が合わないため）
    - `data-aff` は `rakuten-hotel` / `rakuten-kw` / `rakuten` / `jalan-hotel` / `jalan` で分け、GA4でどの型が押されたか見られる
  - **アフィリエイト表記**（景表法ステマ規制）: フッターの `footer-affiliate-note`（`renderFooter()`・`STATIC_FOOTER`・静的フッターを持つ19ページ。文言「当サイトはアフィリエイト広告（楽天・じゃらんnet・Amazonなど）を利用しています」）とプライバシーポリシー「3. 広告について」。**プライバシーポリシーには Amazon の定型文「Amazonのアソシエイトとして、当サイトは適格販売により収入を得ています。」を入れてある**（Amazon提携審査の必須条件。消さないこと）
  - **もしもアフィリエイト**（会員ID 1189544・2026-09-10にメディア「わんさかんさい」を登録・審査中・**shop_site_id=688281**）: 楽天市場の商品購入（promotion_id=54）は**即時提携済み（成果2%・2026-09-10承認メール受領）**。**Amazon.co.jp（promotion_id=170）は2026-09-14に否認**（理由の個別回答は無し。条件の「10記事以上」に対しブログ3記事しか無いのが有力。記事が増えたら再申請する）。物販枠は現状**楽天アフィリエイト直のID**で貼っており、もしも経由には切り替えていない（動いているものを変えない。Amazonが通ったらもしもの「かんたんリンク」で楽天・Amazon両対応にするか検討）
  - **★クリック計測は `js/common.js` の委譲リスナー1か所だけ★** 他ファイルで `addEventListener` すると二重計測になる。ボタンに `data-aff` / `data-aff-pref` / `data-aff-page` を付ける
  - **★楽天は「リンクを押した人が24時間以内にカートに入れ89日以内に購入」すれば別商品でも成果★** 宿の予約に至らなくても成果が出る。だから**押される場所を増やすことが効く**
  - **宿の施設ページへ直リンクする時は、実際にページを開いて施設名・所在地・ペット可否を確認してから**。確認できない宿はエリア単位のリンクに落とす
  - **A8リンクはサイト非依存（アカウント共通）**＝うちどころ/わんさかんさいで同一a8matでよい
- 独自ドメイン: wansakansai.com（お名前.comで取得済み、Aレコード+AAAAレコード設定済み）

## スポットデータ項目
名前、住所、GPS座標、駐車場、トイレ（洋式/和式）、ドッグラン（エリア分離有無・詳細）、入場料、`visited`フラグ（実訪問済みマーク）、`aliases`（別名リスト、検索・危険情報照合に使用。ひらがな読み・略称・通称を含む）、`tags`（汎用タグ配列: sakura, koyoなど）、備考、公式URL

### 駐車場の座標（`parking.lat` / `parking.lng`・任意項目・2026-08-31導入）
運営がGoogleマップ上でピンを刺して取得した**駐車場の実測座標**。入れると地図のピンが駐車場を指し、ズームがz=17に上がり、注記が「地図のピンは駐車場の位置」に変わる。無ければスポットの`lat`/`lng`にフォールバックする（z=16・「おおよその位置」表記）。
- **利用者が本当に知りたいのは「川」や「公園」ではなく「どこに車を停めるか」**。登山口を兼ねる川や広い公園など、スポットの中心と駐車場が離れる場所ほど効果が大きい
- 取得方法: Googleマップで駐車場を右クリック（スマホは長押し）→ 表示された座標をコピー。小数6桁で保存する
- **★全件に入れるものではない。Googleマップの検索で出てこない場所に絞る★**　Googleが特定できない場所だけ座標が要り、検索で出る施設は `lat`/`lng` で足りる。
  - **未設定を宿題として積み上げないこと**。困る場所が出てきた時に足せばよい
- 現在の登録は水越川の1件のみ。他は未設定でよい（フォールバックで正しく動作する）
- 駐車場が複数ある場所は主に使う1か所を入れ、他は`remarks`で補足する

### 犬が入れる範囲（`dogArea`・任意項目・2026-08-31導入）
**「主役が屋内で、犬は屋外エリアのみ」の施設に `"outdoor-only"` を付ける。** 付けるとタイトルが「犬連れは屋外のみ」、meta descriptionが「犬連れスポット（屋外エリアのみ同伴可）」に変わる（`generate_spot_pages.py` の `build_title` / `build_description`）。無ければ従来どおり「犬連れOK」。

**★`"carry-only"`＝「犬を地面に降ろせない」施設（2026-09-13追加）★** 抱っこ・カート・キャリーでのみ入れる施設に付ける。タイトルが「犬連れは抱っこ・カートのみ」、meta descriptionが「犬連れスポット（抱っこ・カートでの同伴のみ可）」になる。**「犬連れOK」と出して現地で降ろせないと分かるのがいちばん困る**ので、検索結果の時点で伝える。付与は `aramaki-rose-park`（荒牧バラ公園・伊丹市公式「ペットが地面に触れない状態で散策される場合はご入園いただけます」）の1件。**ショッピングモール系（ららぽーと・アウトレット等）は同じ型になる見込みなので、追加する時はこの値を使う**（`dogSize` は「地面に降ろせない」という条件であってサイズ制限ではないため、記載が無ければ両方 true のままにし、条件は remarks に書く）
- **判断基準は「犬連れで行って、その施設の目的を果たせるか」**。屋内が主役なら果たせないので付ける
- **一部エリアのみ不可は付けない**。大阪城公園（天守閣内のみ）・彦根城（天守・櫓のみ）・三井寺（堂内のみ）・あわじ花さじき（テラス棟のみ）などは主役が屋外なので「犬連れOK」で正しい
- 付与は `toretore-ichiba`（とれとれ市場 南紀白浜）の1件のみ
- 境界的な例（道の駅うずしお＝付けていない）はCLAUDE_history.md
- **『犬連れ』の語はタイトルに必ず残す**（「○○ 犬連れ」クエリでの検索意図を取りこぼさないため）

### 犬のサイズ（`dogSize` / `dogRun.maxSize`・2026-09-21にフィルター化・ユーザー判断）
`dogSize.{small,medium,large}` は**「そのサイズの犬がそのスポットの目的を果たせるか」**。`false` は「入れない」と確認できた時だけ。項目が無ければ入れる扱い。**ドッグランだけの制限は `dogRun.maxSize`（small|medium|large）に分けて持つ**（公園は歩けるがランは小型専用、という型を丸ごと「大型不可」にしない）。トップの絞り込み「犬のサイズ」（中型犬OK／大型犬OK／小型犬のみ）は dogSize を見て、**「ドッグラン」と同時に押している時だけ maxSize も見る**。`small-dog-only` タグは `dogSize.medium=false` と必ず一致させる。判定は `js/common.js` の `dogSizeAllows` / `dogRunSizeAllows` / `dogSizeWarnings` と `generate_spot_pages.py` の2か所にあり、片方だけ直さない。**エリア分けの境目（10kg未満は小型エリア等）は制限ではない**ので dogSize に反映しない。数字（10kg・15kg・体高50cm）は remarks に残す。抱っこ・カート限定（`dogArea=carry-only`）はサイズ制限とは別だが、長谷寺のように受付から先が抱っこ必須で現実に中型以上が無理なら medium=false にする

### 期限付きの情報（`temporary`・任意項目・2026-09-09導入）
工事やイベント期間の運用のように「**いつ終わるか分かっている**」情報を、remarksではなくここに持たせる。**期限を過ぎると表示が自動で止まる**（`js/spot.js` と `generate_spot_pages.py` が `until` を見ている）。

```json
"temporary": [
  { "note": "臨時駐車スペースは9:00〜17:30の利用で2時間を超えると1回500円",
    "until": "2027-01-31", "source": "https://…",
    "onExpire": "parking.free を true（無料）に戻す" }
]
```

- **remarksには期間限定の情報を書かない**（remarksは恒常的な内容だけ。上の「陳腐化する情報は書かない」の受け皿がこれ）
- **★自動削除はしない★** 施設の期間限定運用は**延長されることがある**。機械的に消すと「延長されたのに消えた」という逆向きの誤りになる。期限の**2週間前**になると `get_next_check_targets.py` がカウントの順番を飛び越えて対象に入れ、`source` の公式を見たうえでタスクが延長・終了を判断する。**ただし前回見た日から3日空いていなければ入れない**（2026-09-20・ユーザー判断。毎日入れると同じ公式ページを見るだけで1日5件の枠を2つ食っていた。見た日は `update_check_count.py` が進捗ファイルの `last_checked` に書く）
- **止めるのは自動、消すのは確認してから。** 期限切れでも表示が消えるだけでデータは残り、`check_spot_expiry.py` が毎日検出して知らせる（前半タスクのSTEP 10）
- 再確認の割り込みは**前半タスクだけ**が行う。後半タスクは `--no-expiry-priority` を付ける（両方でやると2週間まいにち同じスポットを2回見ることになる）
- `note` は**括弧で終わらせない**（表示時に「（2027年1月31日まで）」が付いて括弧が重なる）
- **2つの型がある（2026-09-12・枚方東部公園の初回運用で整理）**。①**状態型**「〜が有料」「〜が閉鎖中」＝ note に日付を書かず、表示側が末尾に「（YYYY年M月D日まで）」を付ける ②**予告型**「10月1日から有料化」＝ note に日付を書く。**表示側は note に `until` の日付（YYYY年M月D日 または M月D日）が含まれていれば末尾を付けない**（`js/spot.js` の `untilSuffix`・`generate_spot_pages.py`）。付けると日付が二重に出る
- **★remarks に同じ告知を書かない★** temporary に入れた時点で remarks からは外す（remarks は恒常的な内容だけ。現行が無料なら `parking.free=true` が詳細欄に出すので繰り返さない）
- **★そのスポットの利用条件が変わらないものは入れない（2026-09-13・ユーザー判断）★** 近隣で開かれるイベントのように、**期間がはっきりしていても料金・駐車場・犬のルールが変わらないなら `temporary` に入れない**（裏の取れない情報になり、表示も濁る）。混雑だけなら remarks の恒常的な記述で受ける。実例＝亀岡夢コスモス園の開園期間と重なる全国都市緑化フェアは、運用が変わる記載がどこにも無かったため入れなかった
- 危険情報の `expires` との違いは「**延長がありうるかどうか**」。気象情報は延びないので自動削除、施設の運用は延びるので確認を挟む

## 地図（スポット詳細ページ）
**★鉄則: 地図は必ず緯度経度で指定する。スポット名や住所の文字列でGoogleに検索させない★**
全スポットが緯度経度を持っているので、文字列検索に頼る理由はない。
- 実装は `js/spot.js` のみ（静的HTMLには地図を出力していない）
- 地図の下に**「Googleマップで開く」ボタン**を置く。埋め込み地図は拡大縮小しかできず、スマホから地図アプリに渡してナビを起動する手段が別に要るため
- 座標は数値型チェック（`typeof === 'number'`）を通してから使う。文字列を直接URLに入れない

> ページ構成の一覧・検索やフィルターの実装の説明・危険情報との照合の仕組み・できることの列挙は、コードが正本なのでCLAUDE_history.mdへ移した（2026-09-17）。ここに残っているのは運用の決まりだけ。

## スポット名検索
- トップページの危険情報バナー下の入力欄。name・aliases・address を部分一致で探し、カタカナとひらがなを相互変換し、中黒・ハイフン・スペースを無視する。都道府県・タグのフィルターと併用可（実装は `js/app.js`）
- **全スポットにひらがな読みをaliasesに登録する運用**（新規追加時も必ず入れる。チェックリスト参照）

## フィルター機能
- 都道府県・条件タグの2段構成でいずれもトグル式（複数選択可・AND条件）。定義は `js/app.js` の `FILTER_GROUPS` にカテゴリごとにまとめる。グループは駐車場（無料/有料/なし）・施設（ドッグラン/トイレ）・料金（入場無料/有料）・特徴（桜/紅葉/水遊び/小型犬のみ/雨でもOK）・宿泊（stay-ok/stay-only）
- **新しいタグを追加する手順**: spots.jsonのtagsに値を追加 → `FILTER_GROUPS` に1行追加
- GPS取得後もフィルターは維持される（フィルター→距離ソートの順で処理）

## デザイン
- モバイルファースト・レスポンシブ
- 温かみのあるアースカラー配色
- フォント: Noto Sans JP

## 許可されている操作
- Bash（git・gh・python・npm等）・ファイルの読み書き・Git操作・GitHub CLI・ローカルサーバーの起動停止・ブラウザでのプレビュー確認は、いずれも確認なしで行ってよい
- Chrome（MCP）でのサイト確認は自由に行ってOK。使い終わったらタブを閉じること
  - **★自動タスクは「閉じた」ことをログに書くこと★** 閉じろという指示だけでは守られない（**記録が無いと閉じ忘れを検知できない**。2026-09-10に実際にタブが残った）。`[tabs] 残りなし` を毎回書き、後始末のSTEPで取りこぼしを回収する
  - **開いた `tabId` を控え、自分が開いたものだけ閉じる。** 一覧から「それっぽいタブ」を推測して閉じると、ユーザーが自分で開いたタブを巻き込む
  - **★残ってしまったタブは、後から誰も閉じられない★** Chrome MCPのタブグループは**セッションごと**で、別セッションのタブは見えない。だから後始末のSTEPで回収する仕組みは**タスクが完走した時しか効かない**。途中で死ぬとタブは残りっぱなしになる
  - **だから「1件ずつ開いて、読み終わったら即閉じる」。** 複数を開きっぱなしにして最後にまとめて閉じる作りにしない。落ちた時に全部残る
  - **開閉は `scripts/track_chrome_tabs.py` に記録する**（`--open` / `--close`）。閉じられない以上、できるのは**次回の実行で気づいて人に知らせる**ことだけ。各タスクのSTEP 0が `--check` で前回の残骸を見つけ、メールで手動クローズを促す
  - **2台以上つながっている時は自分で選ぶ**（2026-09-14）: `isLocal` かつ Windows のものを `select_browser` に渡す（ペアリング要求を出さないので確認不要）。選べないと判断して丸ごと飛ばした実例あり

## 公開URL
- 独自ドメイン: https://wansakansai.com（メイン）
- GitHub Pages: https://imotan-lab.github.io/wansakansai/（独自ドメインにリダイレクト）

## 作業フロー
- コード変更後は必ず git commit → git push する
- **★コミット・プッシュ前にユーザーの確認を取らない（2026-08-31にユーザーから明示指示）★**　変更→生成スクリプト実行→検証→コミット→プッシュ→結果報告、までを一度に行う。ユーザーは移動中にやり取りすることが多く、一手ごとの許可取りは往復が増えて進まないため
  - **例外**: 取り消しにくい操作は従来どおり事前確認する。スポット削除（`scripts/check_x_posted.py` の手順あり）・ユーザーのデータファイル削除・印刷など
  - **確認不要なのは作業の確定操作であって、方針の決定ではない**。掲載を取りやめるか、ルールを変えるか等の判断は手を止めて相談する
- **スポットを追加・編集・削除したら** 以下2つを順に実行してからコミットすること
  - `python generate_spot_pages.py` で `spots/{id}.html` 静的ページを再生成（SEO的に必須）
  - `python generate_sitemap.py` で sitemap.xml を再生成
- **★スポットを削除する前に必ずX投稿履歴を確認する（2026-08-31追加）★**
  ```bash
  python scripts/check_x_posted.py {スポットID}
  ```
  **サイトから消してもXのポストは残る**ため、犬NGを理由に削除したスポットが「犬連れOK」として
  Xに残り続ける事故を防ぐ。該当があればXのポストも削除するか判断すること（該当ポストは
  https://x.com/wansakansai から履歴の日付で探せる）。
  - X本体の検索より確実: 危険情報の投稿は本文に「場所」しか載らないため本文検索では拾えず、
    検索インデックスの遅延や表示制限にも影響されない
  - `--orphans` で「spots.jsonに無いのに投稿済み」＝過去の削除漏れを一括検出できる
  - 履歴は `scripts/x_post_history.json`（.gitignore対象・スポットID/種別/日時/本文を保持）
- スポット詳細ページのURLは `https://wansakansai.com/spots/{id}.html` が正規（旧 `spot.html?id=xxx` は自動リダイレクト）。内部リンクは `spots/{id}.html` 形式で書く
- spots/ 配下のHTMLは `generate_spot_pages.py` の出力なので**手動編集禁止**。再生成で上書きされる（`spots/index.html` も同スクリプトが生成する全スポット一覧ページ。削除対象からは除外済み）

## 静的内部リンク（インデックス対策・2026-08-25導入）

**★鉄則: スポットページへの導線をJS描画だけに依存させないこと★**
JS描画だけだとレンダリング前HTMLにリンクが残らず、Googleから孤立ページ扱いされる。

現在の静的クロール経路（いずれも `generate_spot_pages.py` が生成）:
- `spots/index.html` … 全スポットを府県別に並べた静的一覧。**クロール経路のハブ**。トップページ（`index.html` の `.all-spots-link`）から静的リンクで到達できる
- 各スポットページ … 「近くのスポット」6件への静的リンク（GPS距離順・ハバサイン）＋一覧ページへの導線。**spot.jsが `#spotDetail` を描き直すため表示は従来どおりで、生HTMLにリンクを残すのが目的**
- **静的フッター（2026-09-02追加）** … 全283ページの `</main>` 直後に `<footer class="site-footer">` を静的記述（spots/はgenerate_spot_pages.pyの`STATIC_FOOTER`定数）。`renderFooter()` は**既存フッターがあれば再利用**する（about/contact/privacy/blogがJS描画のみで生HTMLにリンクゼロだった）
  - **★スクリプトタグより前に置くこと★** `</body>`直前だと`common.js`実行時にパーサーが未到達で**フッターが2つ描画される**（実際に発生）
  - `themes/index.html` の `#themeGrid` にも7テーマへの静的リンクあり。テーマ追加時は `js/themes-data.js` とあわせてここにも足す
- 新しい一覧・カテゴリ系ページを作る時も、リンクをJS描画だけに置かない（生HTMLに必ず残す）
- プッシュ後はデプロイ完了を待ち、本番サイト https://wansakansai.com を確認する
- 本番確認はプレビューツールのヘッドレスブラウザで行う（Chromeに干渉しない）
  - preview_startでローカルサーバー起動 → preview_evalで本番URLにナビゲート → preview_screenshotで確認
- 確認結果を報告し、ユーザーにも確認してもらう
- 絵文字は使わない（AI特有の表現を避ける）

## コンテンツ方針
- スポット追加は無料で行ける場所（入場無料・駐車場無料）をメインに集める
- 有料スポットも掲載OKだが、無料スポットを優先
- **宿泊オンリー施設は原則掲載しない**（日帰り利用可能な施設のみOK）。ただし運営者が実際に訪問・宿泊した場所は例外的に掲載OK
- 大阪府のスポットをメインに充実させる（他府県も掲載OKだがサブ）
- スポット画像は自分で撮影したものを使用（ネット上の画像は著作権NG）
- **★掲載する写真は横長に統一する（2026-09-15・ユーザー指示）★** 縦向きで撮ったものは**原本から4:3で切り出して**横にする（サイトの1200px版から切ると荒れる）。撮影も横向きで
- 写真をまとめて渡してもらい、Claudeが良いものを選んでサイトに反映する
- **人が写っている写真は、顔をぼかせば載せてよい**（2026-09-23・ユーザー確認「人写っててもいいからね モザイク処理さえすれば。今までもそうだった」）。人が写っているだけで情報のある写真を外さない
- **★写り込みは切り抜きで外さず、全体を使ってぼかす（2026-09-23・ユーザー指摘）★** 人や車を避けて上側だけ切り取った入園料の写真を「不自然」と言われた。構図は元のまま、顔とナンバーだけを処理する
- **★車のナンバーもぼかす（2026-09-23・ユーザー指示）★** YuNetはナンバーを拾わないので、元画像に座標の目盛りを重ねて拡大し、前後が見えている車を1台ずつ目で探す。処理はモザイク。比較画像で判読できないことを確かめる
- **★傾いた写真は回転して水平に直す（2026-09-23・ユーザー指示）★** 建物の縦線の角度を測って回転し、回転でできた黒い縁は内接する4:3で切り落とす。とよのコスモスの里のトイレ小屋が右下がりに約8.5度傾いていた
- **★画像を差し替える時はファイル名を変える★** 本番の画像は10分間キャッシュされるので、同じ名前で上書きすると見る人の手元で古い写真が残る（2026-09-23に「まだこつぶが写っている」と言われた）
- 顔ぼかしスクリプト: blur_faces.py（YuNet。モデルは `.claude/face_detection_yunet.onnx` を自動検出）。手順は `detect_faces_yunet(img, score_threshold=0.3)` → 赤枠の可視化画像をReadして**自分で目視判別** → `blur_face_ellipse()` でぼかす → 元と処理後を並べた比較画像をReadして自己確認。**低スコア（0.5未満）と顔が見えていない検出はぼかさない**（誤ぼかしの主因）。**可視化・比較画像は長辺1500px以下にリサイズしてからRead**（長辺2000px超を複数Readするとエラーになる）。7ステップの全文・よくある誤検出の一覧・やってはいけないことはCLAUDE_history.md

## Codexによる独立検証（共通ルール）
危険情報・スポット更新・新規スポット追加のいずれも、別のAIに独立して調べさせて結論を突き合わせる。**同じClaudeをもう1体立てるより、別のAIのほうが見落としが減る**ため。ここに書くのは3つに共通する決まりで、対象・件数・タイムアウト・使うスクリプトは各節と各SKILL.mdに書く。

- **★Codexは検証役であって上位の判定者ではない★** 不一致は「Codexが正しい」ではなく**「2つのAIで結論が割れた」状態**として扱う。どちらが正しいかは決めつけない
- **★『Codexが言っているから』を根拠にしない★** 指摘は必ず**自分で確かめ、納得した内容だけを採る**。ログ・メール・コミットメッセージには「Codexが指摘したから直した」ではなく**自分が確認した根拠**を書く。Codexが間違っていることもある
- **★Codexに「Claudeがどう判断したか」を見せない★** 見せると引きずられて、いちばん拾いたい「Claudeが見落とした項目」が出てこない。渡すのは**一次情報と検証前のデータだけ**で、結論・修正内容は渡さない
- **★プロンプトは手書きせず生成スクリプトに作らせ、ファイルに書いてパイプで渡す★** `cat プロンプトファイル | codex exec --skip-git-repo-check -`（シェル差し込みガードでコマンド直書きはブロックされる）。手書きすると「結論に引きずられるな」等の前提を書き忘れた回だけ検証が甘くなり、それに気づけない
  - プロンプトに必ず入れる: 「Claudeの結論に引きずられず自分で調べること」「実際にページを開いて確認したURLだけを根拠にすること」「分からないものは分からないと書くこと」「項目ごとに一致／不一致／確認できずを明記すること」
- **判定**: 一致→確定 ／ 確認できず→確定して記録 ／ **不一致→①まず公式で最終確認して自分で決着を試みる ②決着したらその結論を採用（Claude側が正しいこともある）③決着しなければ変更を保留し、`[codex-diff]` を記録して両方の主張と根拠URLをメールに併記して人間が判断する**
- **★修正そのものをCodexに見せる2巡目を必ず通す（2026-09-20・ユーザー方針）★** 1巡目はHEADだけを見るので**Claudeの修正は誰も検証していなかった**（牛滝山で、存在する出典を「無い」と書き換えた変更が素通りしてサイトに載った）。1巡目が終わった後に修正の一覧（前・後・理由・出典）を `scripts/build_codex_change_review_prompt.py` で渡し、項目ごとに支持／不支持／確認できずを取る。**不支持は一致するまで往復する。回数で打ち切らない。** 3巡で揃わなければその項目は公開せず、両論と根拠URLをメールで人に渡す（3は打ち切りではなく人に渡す合図）。手順は am/pm の STEP 7.5・danger の STEP 5.9
- **★「不一致」と「確認できず」を必ず分ける（2026-09-04・実運用2日ぶんの結果から）★** **不一致＝違う内容を実際に確認できた時だけ。「探したが見つからなかった」は確認できず。** 不一致は公式を開き直して決着を試みる扱いなので、見つからなかっただけのものを不一致にされるとその時間が丸ごと無駄になる。**プロンプトにこの定義を必ず書くこと**（危険情報は生成スクリプトが自動で入れる。スポット再検証は各SKILL.mdの指示に従って手書きする）
- **★こちらの編集方針は、検証役に伝えないと毎回不一致になる★** `expires` は「予報の終了日＋2〜4日の余裕」を入れる決まりだが、それを伝えていなかったためCodexは終了日そのものを答え、気象の情報では必ず不一致になっていた。**方針が絡む項目は「事実の質問」に翻訳して聞く**（「いつ終わるか」を聞き、余裕はこちらで足す）
- **「確認できず」単独ではメールを送らない**。現地看板が根拠のスポットは構造的に確認できずになりやすく、毎回送ると「静かなのが正常」が崩れる。ログには残し、他の条件で送る日に本文へ含める
- **★Codexが落ちてもタスクを止めないこと★** 機械チェックを通った内容で確定し `[codex-skip]` を記録してメールに明記する。検証できないことを理由に更新を止めると「タスクがサイレントに停止」を自分で作り出すため
- **出力を tail や head で切らない**（前半が失われて判定が読めなくなる）
- **★Codexが見られない一次情報は、Playwrightで素材を取って渡す（2026-09-10・ユーザー提案）★** Codexは**Instagramを取得できない**。小規模施設はInstagramが実質の公式なので、**いちばん裏取りが必要なスポットだけ検証が効かない**状態だった。`scripts/fetch_instagram_assets.py` がプロフィール文・ページ画像・投稿画像を保存し、`codex exec -i` で画像を渡す。**★`-i` の後ろにプロンプトを置かない★**（可変長引数なので飲み込まれ `No prompt provided via stdin.` で落ちる。プロンプトは必ずstdinで渡す）。渡すのは一次情報であって結論ではないので「判断を見せない」原則には反しない。ただし**完全な独立検証にはならない**（こちらが取った範囲しか見られず、取り漏らした部分は両者とも見落とす）ので、限界を承知で「ゼロよりまし」として使う
  - **Chrome MCP経由では画像を取り出せない**（2026-09-10に全手段を試して確認。内訳はCLAUDE_history.md）。だからPlaywrightを使う。**`--help` にオプションがあることと、実際に使えることは別**
- **無人タスクでもワークフローとエージェント起動は禁止**。Codexは承認プロンプトを持たない単発のサブプロセスなので例外として許可している
- 数分から10分以上かかるためバックグラウンドで走らせてよい


## 自動タスク共通方針
- **★SKILL.mdは目的を書いたものであって、機械的に従うチェックリストではない（2026-09-10・ユーザー方針）★** 「何件まで」「何回まで」と数で縛るより、**何を探しているのかに照らして自分で判断する**方がよい。実行するのはAIなので、目的が書いてあれば止めどころも判断できる。
  - 悪い例: 「投稿のキャプション全文まで読む」…**どこで止めるかの判断材料が無い**。「最大10件まで」も機械的で、11件目の告知を取りこぼす
  - 良い例: 「探しているのは料金改定・定休日の変更。日常投稿が続くだけなら答えは無い。1年以上前に達したらどのみち採用できないので遡らない。**判断の根拠をログに書く**」
  - **数で縛るのは、判断の余地が無い機械的処理だけにする**（対象件数・タイムアウト・リトライ回数など）
- **★「うまくいかない時どうするか」を必ず書く（2026-09-10・全SKILL.mdを点検して確立）★** 手順に**逃げ道が無いと、実行側は粘り続けて時間を使い切る**。2026-09-10のpm停止も、weekly-seoの行数変更（ピクセル指定のクリックに依存＝画面が変われば失敗する）も同じ構造だった。
  - 書き方: **①何回か試す ②だめなら部分的な結果で先へ進む ③ログとメールに明記する**。「必ず〜してから次へ進む」とだけ書いて、できなかった場合を書かないのが最悪
  - **部分的でも進める根拠を添える**と判断しやすい（例: 累積CSVは同じ日付を後から上書きするので、今週の欠損は翌週に埋まる）
  - 点検の観点は「失敗しうる操作か」ではなく「**失敗した時の行き先が書いてあるか**」。Chrome未接続・Codex不通・X投稿失敗はいずれも行き先が書いてあり、実際に事故を起こしていない
- **SKILL.md frontmatterには `model:` 行を入れない**（最新Opus 1Mに自動追従させる方針）
  - 確認方法：各タスクログの「使用モデル」行で実際の実行モデルを確認（意図せずSonnetになっていたら要再検討）
  - 対象タスク（5つ）: wansakansai-daily-spot / danger-update / spot-update-am / spot-update-pm / weekly-seo
- **★スポット更新タスクのメールは「これをこう変えた」を出す（2026-09-14・ユーザー指摘）★** spot-check の --fixed はスポット名を並べるだけで、**何をどう変えたのかはログ全文から探す**しかなかった。--changes-file と --pending-file にJSONを渡すと 前→後 と理由・出典が本文の先頭に出る。**項目単位で1要素。before は足した項目なら「（記載なし）」。why は自分が確認した根拠を2〜3文で。「判断してほしいこと」は答えで動きが変わるものだけ（報告は入れない）。**書き方は am/pm の SKILL.md「STEP 9」。ログ全文は本文に付けない（ユーザー不要）
- **★メール送信ポリシー「静かなのが正常」（2026-07-09適用・うちどころ2026-07-08方式）★**: メールを送るのは「①人間の対応が必要 ②公開内容が変わった ③異常（エラー・スキップ・中断）」の時だけ。変化なしの日は送らず、ログに「メール送信なし（変化なし・全OK）」を必ず記録する。各タスクの具体的な送信条件はSKILL.mdの「★メール送信ポリシー★」ブロック参照。**weekly-seo（毎週金曜）はメール自体が成果物の受け渡し手段なので毎回送る（ポリシーの対象外）**。**週次ハートビートは weekly-seo（金曜）と task-watchdog の🟢正常メール（月曜のみ）が担う**（この役目を持っていた weekly-digest は2026-09-08に廃止。理由はCLAUDE_history.md）。SKILL.mdの送信条件を変更したら必ず敵対的レビュー（別エージェント・最低1巡）を通すこと
- **★無人タスクのコミットメッセージは `-F` でファイル渡し★** Write でメッセージをファイルに書いて `git commit -F` に統一（am/pm/danger-update）。`-m` に複数行を直書きすると PowerShell の here-string の記号が件名に混入する。**`--amend`/`force push` は「直前の自分の1コミット・メッセージのみ・`--force-with-lease`」の3条件が揃った時だけ。** 外れる状況（人のコミットが上にある・中身を変えたい・hashが合わない）では新しいコミットを重ねるか、失敗として人に渡す（経緯はCLAUDE_history.md）
- **全5タスクはログ末尾に完了マーカー「=== タスクID 完了 ===」を必ず書く**（エラー中断時は書かない）。task-watchdogのSTEP 2.5「完走点検」がこのマーカーで「起動したが途中で黙って死んだ」型を検知する（メール不在が異常シグナルでなくなった分の補完。spot-update-am/pmはspot_check_YYYY-MM-DD.logを共有するためタスクID入りマーカー必須）
- **★スポットは「修正した分」ではなく「対象5件すべて」をCodexに見せる（2026-09-07に変更）★** **間違って直せばCodexが気づくが、間違って見逃しても誰も気づかない。** 見逃されたスポットはチェック回数が+1されて順番が後ろに回り、長期間放置される。**修正が0件の日もCodexを呼ぶ**。呼び出しは1日最大2回（am・pm）で、**呼ぶのは書き込む直前の1回だけ。1件ずつ呼ばない**（1施設10分以上かかりタスクが破綻する）。`scripts/build_codex_spot_prompt.py` が**修正前（git HEAD）のデータ**だけを渡す（STEP 4で作業ツリーは編集済みのため、HEADが「前日の公開状態」になる）。突き合わせは4通りで、**「Claudeは問題なし／Codexは要修正」が2026-09-07まで一度も見えていなかった型**。タイムアウトは20分（5件ぶんに増えたため600秒から引き上げ）。**所要時間は項目数では決まらない**（2項目で6分／9項目で約3分の実測）。手順の詳細は am / pm のSKILL.md「STEP 8.5」。共通の決まりは上の「Codexによる独立検証」
- **CLAUDE.mdサイズ検知**（wansakansai-spot-update-am が日次実行）: `python scripts/check_claude_md_size.py --json` が閾値超（**80KB**・2026-09-02に50KBから引き上げ。★この数字はClaudeが置いた目安であって根拠のある上限ではない。守るために文章を削るのは本末転倒で、本来見るべきは「書いてあるルールが今も必要か」★）・履歴参照喪失をNG判定する。NG時のみメール通知。★無人タスクはCLAUDE.mdを書き換えない（圧縮は対話セッション専用＝冒頭「変更履歴について」の圧縮手順参照）★
- **★STEP 0で作業ブランチと作業ツリーを確かめる（2026-09-17・am/pm/danger）★** master でなければ切り替える（別ブランチのまま pull すると意図しないマージを作り、直した内容も公開ブランチに載らない。2026-09-16に発生）。**未コミットの変更は前の実行が途絶した証拠。**捨てず引き継がず、自分で裏取りし直して今日のぶんと一緒に公開する。「Codexを呼ぶ手前で落ちた」型は pending_codex_catchup.py が開始/完了マーカーの対応で検知する（codex-skip の印だけを見ていた頃は0件と答えていた）。**★このスクリプトを変えたら必ず `python scripts/test_pending_codex_catchup.py` を流すこと★**（2026-09-17のCodexレビューで見つかった9件の誤りを、1件ずつ「どの向きに間違えるか」付きで固定してある。ログは複数のタスクが1ファイルを共有し同じタスクが1日に2回走ることもあるため、**日でひとまとめにすると別の実行の印を自分の印として数える**）
- **死活監視は task-watchdog（毎朝8:03・うちどころ側で一元管理）が登録されている全タスクを点検・自動復旧する**（対象は list_scheduled_tasks から動的に取るので、タスクを増やしても番兵側の変更は要らない。2026-09-03時点の内訳はうちどころ2＋わんさかんさい8）。わんさかんさい側で同じ死活監視を二重に作らないこと（重複は無駄＆混乱の元）。詳細はグローバルCLAUDE.md参照
  - 共有スクリプトの正本バックアップは `Claude_backup/スクリプト/` に集約され、task-watchdogが毎日自動更新する（手動バックアップ不要。わんさかんさい側で個別に backup 対象へ追加しなくてよい）

## スポット情報 定期チェック（自動タスク）
- 毎日 昼12:10（前半5件）と深夜2:37（後半5件）にローカルスケジュールタスクで実行。カウント制ローテーションのため実行順は影響しない
- **調べる役と直す役は1本のタスクにまとめてある**（2026-09-07に統合。独立性はCodexの独立検証が担う。経緯と旧SKILL.mdの退避先はCLAUDE_history.md）
- タスクID: wansakansai-spot-update-am（前半・昼12:10）/ wansakansai-spot-update-pm（後半・深夜2:37）
- **チェック優先度はカウントベース**（`check_counts` で回数を管理し少ない順。新規は初期値0で最優先）。対象取得は `scripts/get_next_check_targets.py`、更新は `scripts/update_check_count.py`。詳細は各タスクのSKILL.md参照
- 複数サイト突き合わせ + ブログ・SNS犬連れ実績確認
- **有料施設・小規模施設はInstagramも確認する**（2026-08-25追加。対象59件・1日10件チェックのうち平均2.2件）。HPを持たない施設はInstagramが実質の公式で、料金改定・定休日がそこにしか出ないため
  - **★`dogRun.free` 単独で対象判定しないこと★** ドッグランが無いスポットもデフォルトで false のため、ほぼ全件が対象になりタスクが破綻する
  - 対象判定と歯止め（投稿日1年以上前は単独採用しない・Chrome MCPで原文未確認なら修正しない・一時的情報は書かない・アカウントの本人確認）の詳細は各タスクのSKILL.md参照
- 問題があればspots.jsonを修正してcommit & push
- タスクの冒頭で全件文体チェック（`python scripts/check_writing_style.py --fix`）を実行し、機械的に判定可能な違反は自動修正する
- 各スポットの本番URLをChrome MCPで開き、レイアウト崩れ・画像404・JSエラーをログに記録する
- 自動タスクの実行時間にClaude Codeローカル＋Chrome MCP接続が必要。Claudeを立ち上げ続けておくこと
- Chrome未接続時はレイアウトチェックだけスキップしてタスクは継続（Web情報チェックは実施）。スキップした分は進捗ファイルの `pending_layout_ids` にプール、次回チェック時に今回分と合わせて消化（チェック成功でpendingリセット）
- ログ: C:\Users\imao_\.claude\logs\spot_check_YYYY-MM-DD.log（自動削除なし）
- 進捗管理: C:\Users\imao_\Documents\wansakansai\spot_check_progress.json
- ログ出力はlog.py経由。log.pyはログ名で出力先を振り分け: spot_check_*とwansakansai_*は.claude/logs/へ、それ以外はDocuments/uchidokoro/logs/へ

## 危険情報（data/dangers.json）
- 毎日自動更新あり（毎朝6:31 JST、ローカルスケジュールタスクで実行。2026-07-17に21時から移動＝夜間セッション制限の回避＋朝の散歩前に更新が届く配置）
- タスクID: wansakansai-danger-update
- メール通知は変化・異常時のみ（send_notify.py wansakansai。「静かなのが正常」ポリシー＝上記「自動タスク共通方針」参照）。**更新の中身は `--changes-file` で 前→後 と理由・出典、判断依頼は `--pending-file` で項目ごとに本文へ入れる**（2026-09-14〜15・書き方はSKILL.md STEP 7。`--skipped` はエラーだけ。件名は中身に合わせて変わる）
- ログ: C:\Users\imao_\.claude\logs\wansakansai_YYYY-MM-DD.log（自動削除なし）
- Dropboxバックアップ: C:\Users\imao_\今電 Dropbox\今電　今尾笙夢\Claude_backup\自動タスク\wansakansai-danger-update\
- 情報源: 自治体公式サイト + SNS（X、Instagram、Threads）
- SNS情報は「SNS上の報告」等と明記し、公式情報と区別する
- 1件だけの未確認情報は掲載しない（複数アカウントからの報告があるもののみ）
- ただし危険情報（毒餌・事故等の注意喚起）はSNS情報であることを明記すれば1件でも掲載OK（注意喚起は誤情報でも気をつけるに越したことはないため）
- 3ヶ月以上前で続報なし・解決済みの情報は削除を検討

### 種別（type）は6語だけ（2026-09-03に確定・2026-09-20に気象を外した）
毒餌 / 毒餌の疑い / 感染症 / 危険な生き物 / 有毒なもの / その他の注意。語彙外は `scripts/check_dangers.py` が弾く（「事故」廃止の経緯はCLAUDE_history.md）。
**★気象は載せない（2026-09-20・ユーザー判断）★** 台風・大雨・残暑・高波は、期間がピンポイントで数日で意味が消え、飼い主は天気を自分で見ている。更新のたびにXへ流れて犬連れスポットの投稿に天気が混ざり、2AI検証でいちばん揉め、書き換え回数もいちばん多かった。検索もせず、追加も更新もしない。犬固有の注意（肉球のやけど等）を置くなら日付で追いかけるエントリでなく定型文で

### expires（表示期限）で数日で終わる情報を自動で消す（2026-09-03導入）
`"expires": "2026-09-09"` を入れると、その翌日から `scripts/expire_dangers.py --apply` が削除する。
- **入れるのは「いつ終わるか分かるもの」だけ**。もともと台風・大雨のために作ったが、気象を載せなくなった2026-09-20以降は出番がほぼ無い。仕組みは残す。毒餌・感染症・危険な生き物には入れない（いつまで危険か決められないため従来どおり3ヶ月ルール）
- expires の書式が壊れている時は消さずに残す（消す方に倒すと事故になる）

### ★公開前に「機械チェック → 2AI検証」を通す（2026-09-03導入）★
危険情報は人の目を通らずサイトに公開され、Xにも自動投稿される。実際に、投稿文が括弧の途中で切れた状態で公開され2日間気づかれなかった。**別の監視タスクを足すのではなく、公開する手順の中に検証を入れる**（後から見つけても、すでに公開されているため）。

**分担を分けること。機械で判定できることをAIに投げない。**
- `scripts/check_dangers.py` … 必須項目・id重複・日付書式・種別の語彙・要約の長さ・**括弧の対応**・HTMLタグ混入・**投稿文の括弧と文字数**。毎回実行し、**NGならコミットもX投稿もしない**
- Codex … 種別が中身に合っているか・期限を入れるべきか・要約が矛盾していないか・事実関係。**判断が要ることだけ**

**★2つのAIが一致した時だけ採用する（2026-09-03・ユーザー指示）★** どちらのAIも信用しない前提で運用する。判定のしかたと不一致の扱いは上の「Codexによる独立検証（共通ルール）」。危険情報だけの上乗せは次の3つ。
- **不一致が決着しなければ2巡目**（Claudeもcodexも調べ直す。`--round 2`）。2巡目でも割れたら**その項目は変更せず**メールに併記する
- **保留の意味**: 種別・期限・要約が割れた場合は中身を公開してよい（項目はClaudeのまま・メールに併記）。**事実関係が割れた場合はそのエントリを公開しない**（誤った危険情報を出す方が害が大きい）
- プロンプトは `scripts/build_codex_danger_prompt.py` が生成する。変更が無い日は終了コード2を返し、Codexを呼ばない

## 週次SEOレポート（自動タスク・2026-09-02導入）
タスクID: wansakansai-weekly-seo（毎週金曜の朝）。ログ: `.claude/logs/wansakansai_seo_YYYY-MM-DD.log`
Chrome MCPでSearch Consoleの「日」タブの画面テキストを取る → `scripts/seo_ingest.py` で累積CSVへ → `scripts/seo_report.py` でPDF/PPTX → メール添付 → Dropbox退避。
**金曜なのはデータ確定が4日遅れだから**（2026-09-02水に実測＝最新は8/29土まで。日曜ぶんが揃うのは木曜以降）。画面の開き方・行数を500にする操作（キーボードの下矢印を使う）はSKILL.md参照。
累積CSV `C:/Users/imao_/Documents/wansakansai/seo_daily.csv` は**ローカルのみ**なので毎回Dropbox（`Claude_backup/自動タスク/wansakansai-weekly-seo/`）へ退避する。成果物も同じ場所。

- **★数字を作らない★** 画面テキストは各項目が区切りなしで連結される（`2026/08/29811,9764.1%9.2`）。**手で切り分けないこと**。`--expect-clicks` に画面の合計を渡して検算し、合わなければ取り込まず終了コード1で止まる
- **★1週間の増減だけで語らない★** 週単位は天気やニュースで簡単に上下する。必ず直近12週の棒グラフと「12週中何番目か」を併記する（前週比マイナス39%でも12週中3番目に多い、という読み方をさせるため）
- **★PDF生成にPowerPointを使わない★** COM自動操作は無人実行に耐えないと実測で判断（詳細はCLAUDE_history.md）。PDFはmatplotlibのPdfPages、PPTXはpython-pptx
- 累積CSVは同じ日付を後から取った値で上書きする（数字は数日かけて確定するため）。直前の状態は `.bak` に1世代残る
- **★Search Console は行数の変更が効かないが、URLのパラメータは効く（2026-09-12実測）★** 表の「1ページあたりの行数」を500にする操作はピクセル指定のクリックもキーボード入力も通らなかった（上位10件しか取れない）。代わりに URL の `&start_date=YYYYMMDD&end_date=YYYYMMDD` で任意の期間、`&query=*語` で語の絞り込みができる。**月ごとに開けば季節ごとの上位クエリが取れる**
- **未着手**: Search Console API（サービスアカウント）にすればChrome未起動でも動く。今はChrome未接続だとエラーメールになる

## X自動投稿（@wansakansai）
- 危険情報・新スポット・ブログ記事・今日のおすすめ・季節特集を自動投稿（週次まとめは2026-09-08に廃止）
  - **★「今日のおすすめ」は検索の需要と写真のあるスポットを中心に選ぶ（2026-09-25・ユーザー依頼）★** 運営者は需要のありそうな場所を選んで写真を撮りに行っているので、投稿もそこに寄せる。ただし同じ場所ばかりにしない。需要は Search Console のページ別表示回数（`Documents/wansakansai/spot_demand.json`・週次SEOタスクが毎週更新）。重みと試算の結果は `docs/x_automation.md`、試し抽選は `python scripts/post_daily_spot.py --simulate 90`。**重みを変えたら試し抽選で「需要か写真のあるスポットの割合」と「同じスポットの最多回数」を確かめる**
  - **新スポットは `scripts/new_spot_queue.json` の先頭から1日1件、daily-spot タスクが今日のおすすめの代わりに投稿する**（2026-09-14・`post_next_new_spot.py`）。`merge_new_spots.py --apply` がキューに足す。並べ替えは手でよい
  - **★X投稿の本文に細かい利用条件は入れない（2026-09-18・ユーザー方針）★** 抱っこ・カートのみ、有料化の予告、開園期間のような条件はサイトに書いてあればよい。**Xはサイトを見てもらうための足がかり**であって、情報の完成形ではない。投稿文に条件が無いことを「要判断」として人に回さない（2026-09-18に荒牧バラ公園の carry-only で1回起きた）
- **★投稿スクリプトは起動し直さないこと★** 投稿前にランダム遅延（0〜60分）が入るため、Bashのバックグラウンド実行が10分あたりで打ち切られたように見えるが**プロセスは生きている**。起動し直すと二重投稿になりうる。状態はstdoutログの `Posting jitter:` / `[post jitter]` の行と `tasklist` で確認する
- 投稿ライブラリ: `C:/Users/imao_/.claude/x_poster.py`（Playwright使用）
- 認証: Cookie自動リフレッシュ方式（タスク実行時だけheadless Chromeを起動）
- 専用プロファイル: `C:/Users/imao_/.claude/x_chrome_profile`（port 9222）
- Cookie失効時は手動で専用Chromeから再ログイン必要
- **詳細仕様は `docs/x_automation.md` 参照**（投稿ロジック・認証・キャッシュクリア・マルチアカウント対応等）


## 危険情報とスポットの連動
- スポット詳細と危険情報ページは、スポット名＋aliases の照合でJSが動的に相互リンクする。JSONを更新するだけでリンクが自動追従する
- **照合は個別ロジックではなく aliases で対応する方針**。スポット追加時、正式名称以外で呼ばれそうな名前は必ず aliases に入れる（道の駅のプレフィックスなし、括弧内の名前、通称など）

## スポット情報の調査ルール
**スポット追加・更新時は必ずこのルールを守ること。1サイト調査で書くと誤情報が大量混入する。**

- 施設情報（ドッグランのルール・体重/体高制限・入場料・駐車場・営業時間など）は**必ず複数サイト（公式サイト＋2サイト以上）で突き合わせてから**spots.jsonに書くこと
- **★新規追加・大量更新時は、Codexによる独立再検証を必須にする（2026-08-31にこの方式へ変更）★** 呼び方・プロンプトの決まり・不一致の扱いは上の「Codexによる独立検証（共通ルール）」。ここでの上乗せは、**不一致の項目は「公式で最終確認 → それでも確定しなければ控えめ表現」で処理する**こと
- **全項目について2回チェック必須**（駐車場・営業時間・料金・公式URLだけでなく、住所・GPS座標・犬連れ可否・tags・aliases・dogSize・dogRun・toilet含めすべて）。1回目はClaudeが調査、2回目はCodexが再検証、という2つのAIで別々に確認する
- 特に以下は複数サイト確認必須:
  - ドッグランのエリア分離・体重/体高制限
  - 駐車場の無料/有料・**具体的な台数**（季節によって変わる場合も注意）
  - 入場料・利用料金（値上がりしやすい、**必ず税込表記で公式の最新ページから取得**）
  - **犬のサイズ制限・犬種制限**（大型犬NG、小型犬のみ等。ドッグランと店内で条件が違うケースあり）
  - **住所の番地まで正確に**（公式サイトと地図サイトで一致確認）
  - **営業時間・定休日**（夏季/冬季で変わる場合も。ナイト営業があるか）
  - **2025〜2026年の値上げ情報**に特に注意
  - **マナーパンツ/ベルト着用必須か、ワクチン接種証明書の提示が必要か**
- **複数サイト + SNS（X、Instagram、食べログ、わんわんスタジアム等）でも必ず突き合わせる**こと。公式だけだと最新の運用実態が反映されていない場合がある（マナーパンツ必須化、料金改定、犬種制限変更など）。SNSや個人ブログで見つかった最新情報も参照して整合性を取る
- 1サイトのみの情報で断定的に書かない。不確かな情報は「要確認」「推奨」など表現を和らげる
- **★裏付けが弱い情報の扱い（2026-08-31確立・Claude/Codexの2AIで合意）★**
  - **実害の大きさは、未確認情報を掲載する理由にはならない**。実害が大きい情報ほど「追加確認の優先度を上げる」理由にする。裏付けが足りないときは**具体値を書かず「事前確認推奨」に留める**
  - **「不明」と「否定」を区別する**。確認できない属性は `null`（未確認）にし、**`false` は「無い・非対応と確認できた場合だけ」**使う。根拠なく `false` を書くのは誤情報
  - **情報の種類で優先順位を変える**: 営業時間・料金・禁止事項など**管理者が決める情報は公式を優先**。所要時間・坂のきつさなど**体験依存の情報は複数の実訪問ソースを目安にしてよい**
  - **公式と実訪問情報が食い違う場合は、一方を黙って捨てず差を併記する**
  - この4点の実例（曽爾高原の駐車場・徒歩時間、屏風岩公苑のトイレ、信貴山の掲載取りやめ）はCLAUDE_history.md「2026-08-31 裏付けの扱いと情報の優先順位」を参照
- **★出典が割れた時は「利用者が損をしない側」に倒す（2026-09-09確立・ユーザー方針）★**
  複数の出典で数値が食い違う時の手順。**まず日付を見る。新旧がはっきりするなら新しい方を採る**（古い資料は「現在の公式案内」ではない＝上の優先順位①に照らせば現行が勝つ）。どちらも現行で同程度に信頼できる時だけ、次のように倒す。
  - **駐車台数は少ない方**。151台と166台で決着しないなら「150台以上」と書く。停められないという事故が起きない
  - **料金は高い方**。安く書くと現地で足りない。**料金に「◯円以上」は使わない**（利用者が知りたいのは実額なので、割れたままなら両方を併記する）
  - **営業時間・開園期間は短い方**。行って閉まっている事故を防ぐ
  - **サイズ・体重制限は厳しい方**。断られる事故を防ぐ
  「150台以上」のように幅を持たせた書き方は嘘にならず規模感も伝わるが、**下限側に丸めた時だけ**成立する。上限側に寄せると、多い方の出典が誤りだった時にそのまま誤情報になる
- **個人ブログ・SNS（X、Instagram等）でも犬連れ訪問の実績を確認すること**。公式サイトに犬OKと書いてなくても、実際に犬連れで行っている人がいれば掲載OK。逆に犬NGの情報があれば掲載しない
- **★情報の優先順位（2026-08-31確立・Claude/Codexの2AIで合意）★** 公式の記載とブログ等の訪問実績が食い違う場合は次の順で判断する:
  ①**現在の公式案内** ②施設への直接確認 ③個人ブログ・SNSの訪問報告
  **公式が範囲を限定せず「ペット不可」と明記している施設は、ブログに「行けた」という報告があっても掲載しない**（ブログが示すのは「その日歩けた」という事実だけで、施設が許可した証明にはならない。見逃されただけ・当時と運用が違う等の可能性がある）。「犬OK」と表示して現地で断られると、利用者は移動時間と交通費を無駄にし、施設側にも迷惑がかかる
  - **一部エリアのみ不可は掲載OK**（例: 大阪城公園の天守閣内・彦根城の建物内・平等院の鳳凰堂内はペット不可だが屋外はリードでOK）。remarksにどこが不可かを明記すること
- **★「犬連れ需要がある場所」と「犬が入れる場所」は別（2026-09-13・秋の新規12件を調べて確立）★**
  検索需要から候補を選ぶようになったので、**候補に挙げた時点では犬可否は何も分かっていない**。必ず公式で確かめてから記事化する。出てきた型は4つ。
  - **花の名所・公園でも全面的にペット不可の施設はふつうにある**（12件中2件が該当し掲載を取りやめた）
  - **★施設本体が犬OKでも、駐車場が犬不可のことがある★**（牛滝山 大威徳寺。いちばん大きい駐車場が公式に同伴不可で、犬連れは民間の有料駐車場を使うしかない）。**「どこに停めるか」まで見ないと現地で詰む**
  - **犬は入れるが水には入れない場所もある**（滝畑は公式が入水不可。川遊びの名所だが `water` タグは付けない）。**タグは「その行為ができるか」で判断する**
  - **一部だけ不可なら範囲を書く**（犬鳴山 七宝瀧寺は公式が「身代わり不動明王前広場まで」と限定）。掲載可だが、どこまで行けるかを remarks に書く
  - 施設名と公式の原文はCLAUDE_history.md
- **★Shift_JIS のサイトは WebFetch だと文字化けする（2026-09-13実測）★** 滝畑湖畔観光農林組合の公式は Shift_JIS で、WebFetch は「犬に関する記述は見当たりません」と返した（実際には書いてある）。**取得できなかったのか、書いていないのかを区別すること。** urllib で生バイトを取り、utf-8 → shift_jis → euc-jp の順に decode を試せば読める
- **★公式サイトを持たない施設はInstagramを一次情報として必ず見ること（2026-08-25追記）★**
  小規模なドッグラン・ドッグカフェはHPを持たず、Instagramで営業時間・定休日・料金・臨時休業を告知しているケースが多い。まとめサイトの情報は古いことがあるため、**Instagramが実質の公式なら officialUrl にInstagramのURLを入れてよい**。
  - **見る手段**: ①`WebFetch` でプロフィール文・固定情報は取得できる（投稿本文は取得できないことが多い）②**Chrome MCP（ログイン済み）なら投稿本文まで読める**。プロフィールの「続きを読む」を押すと定休日・住所などの全文が出る。投稿を開けばキャプション全文が読める
  - Chromeを使ったら**タブを必ず閉じる**（既存ルール）
- **★調査を人に頼む時・Codexに投げる時は、上のチェック項目を具体的に列挙して伝えること★**（抽象的な指示だと見落とす）。「複数サイトを突き合わせて不一致を報告せよ」「ブログ・SNSでの犬連れ訪問実績も確認せよ」「remarksは200字以上」を必ず含める
- **★最上位はユーザーの実体験。ただし実体験も時間が経てば古くなる★**
  - ユーザーが見た・体験したことは、ネット情報やAIの調査結果より優先する。CodexとClaudeが何と言おうとユーザーの体験が上
  - ただしそれは**「そのとき、そうだった」という事実**であって、**「今もそうである」保証ではない**。施設のルールは変わる
  - **★季節や潮位で運用が変わる場所はとくに公式の現行案内を見る★** 海水浴場・干潟・海辺のスポットはシーズン中だけペット規制がかかることがあり、駐車場も無料から有料に変わる。冬季閉鎖・季節営業も同様
  - **食い違ったら実体験を捨てない**。「訪問時はこうだった」と**時期を添えて書き**、現行は公式で確認するよう促す（例: 江津良海岸はオフシーズンに無料駐車場3台。海水浴期間は有料の駐車場が開く）
  - ネット情報がユーザーの体験と食い違う場合は、ネット側を修正・削除する

## スポット追加時のチェックリスト
追加・更新のたびに確認する。

- [ ] **Codexで独立再検証したか**（新規追加・大量更新時は必須。自分の調査結果を信用しない）
- [ ] **全項目について2回チェックしたか**（駐車場・営業時間・料金・公式URLに限らず、住所・GPS・犬連れ可否・tags・aliases・dogSize・dogRun・toilet含めすべて）
- [ ] 複数サイトで情報を突き合わせたか（公式＋2サイト以上）
- [ ] 個人ブログ・SNSで犬連れ訪問の実績があるか（犬NG情報がないか）
- [ ] `tags`の付け忘れがないか … `sakura`桜の名所 / `koyo`紅葉の名所 / `water`川遊び・水遊び可 / `small-dog-only`小型犬のみ / `rain`屋内・屋根付きあり（タグを増やしたら本リストも更新）
- [ ] `aliases`に通称・略称・別名が漏れていないか
- [ ] `visited`フラグが正しいか（実訪問済みならtrue）
- [ ] `officialUrl`が正しいか（公式サイトまたは公式観光情報サイト）
- [ ] `remarks`の文体が下記の「文体ルール」に沿っているか

## remarks（備考）の文体ルール
スポット情報の `remarks` フィールドは下記のルールで統一する。新規追加・更新時は必ず守ること。自動チェックも走るが、書く時点で意識する。

### 基本方針
- 常体＋体言止め中心。「です・ます」「だ・である」は使わない
- 1段落で完結。改行・箇条書きはしない（複数の短文を「。」で繋ぐ）
- 冒頭は施設の性格・規模を一言で（例:「24時間365日利用可能な無料ドッグラン。」「徳川御三家の城。」）
- 末尾は必ず「。」で終わる
- **分量は必ず200字以上**（薄い解説を作らない。150字未満は不可）。施設の性格・規模・犬連れ条件（リード/エリア分離/サイズ制限/ワクチン等）・駐車場・トイレ・料金・営業時間・注意点を盛り込んで厚みを出す。**スポット追加の調査時およびCodexへの検証依頼にも必ずこの200字以上基準を明記すること**（指示し忘れると薄い解説が混入する）
- **★陳腐化する情報は書かない（2026-08-25追加・ユーザー方針）★** remarksには**有効期限の概念が無く、古い情報が残り続けると誤情報になる**ため、次は書かない:
  - **個別のメニュー名・季節限定メニュー**（「梅しそロールカツ」「かき氷」等）。頻繁に変わる
  - **臨時休業・期間限定価格・今シーズンだけのイベント**
  - **「◯月◯日から」という日付付きの告知**
  書いてよいのは**恒常的な事柄**のみ（料金体系・定休日・通年運用の季節別営業時間・設備・犬連れ条件）。季節で変わるものは「**夏季は犬用プールあり**」「**季節ごとにメニューが変わる**」のように**具体名を出さない表現**に留める。最新情報が要る施設は「最新情報は公式Instagram等で確認のこと」と結ぶ
  **★終了日がはっきりしているものは、捨てずに `temporary` に入れる（2026-09-09追加）★** このルールは「remarksに有効期限の概念が無い」ことが理由だったので、期限を持てる置き場ができた以上、工事・イベント期間の運用のように**いつ終わるか分かっている情報は載せてよい**（詳しくは上の「期限付きの情報（`temporary`）」）。終わりが読めないものは従来どおり書かない

### 表記
- **句読点**: 全角「、。」のみ。半角「,.」は数字内のみ可（「1,325頭」「2.4km」「105.6ha」）
- **括弧**: 全角「（）」のみ。半角「()」は使わない
- **並列**: 中黒「・」（「狂犬病・混合ワクチン」「桜・紅葉」）
- **範囲**: 全角波ダッシュ「〜」(U+301C)。「～」(U+FF5E 全角チルダ)、「-」(半角ハイフン)、「~」(半角チルダ) は使わない
- **数字・単位**: 半角（「8kg」「100平米」「410円」「9:00〜17:00」）
- **時刻**: 「9:00〜17:00」形式（半角コロン+全角波ダッシュ）

### 頻出フレーズ（既存スポットでの統一表現）
- 入場系: 「リード着用」「ペット不可」「犬同伴OK」「全身が入るキャリーバッグ利用で〜可」
- 注意系: 「事前確認推奨」「〜の場合あり」「要注意」「自己管理」「持参のこと」
- 設備系: 「〜エリアと〜エリアの2区画」「エリア分離」「アジリティ設備あり」
- 料金系: 「平日〇〇円・土日祝〇〇円」「無料駐車場〇台」「〇〇円（税込）」

## スポット写真
- **★情報サイトの素材として選ぶ（2026-09-14・ユーザー指示）★ 愛犬が主役で背景に施設の情報が無い写真は載せない**（ブログではなく情報サイトのため）。**背景に入口・看板・設備・景色が写っていれば犬が写っていてよい**。同じ構図の重複を並べない。**人が写っていても、顔をぼかせば載せてよい**（2026-09-23・ユーザー確認。以前ここに「顔をぼかしても載せない」と書いていたが、それは播州清水寺のドッグランの1枚について言われたことを私が一般ルールに広げたもので、誤りだった）。現地の掲示でも、無料Wi-FiのSSIDとパスワードのように**再配布になるもの**は載せない
- spots.jsonの`images`フィールドに複数画像パスを配列で指定（`imageUrl`は1枚目と同じ値）
- 画像は `images/spots/{スポットID}/` に配置
- スポット詳細ページにギャラリー表示あり（矢印ナビ + サムネイル切替）
- Web用: 4:3の横長・長辺1200px・JPEG品質85（500KBを超えたら5ずつ下げる）・EXIFなし

## ブログ
- 記事一覧: `blog/index.html`（公開済み、ナビから遷移可能）
- 記事ページ: `blog/` ディレクトリに個別HTMLで配置
- ブログ用画像: `images/blog/{記事スラッグ}/` に配置
- 下書き: `blog/drafts/` にMarkdownで保存
- **現在3記事公開済み**（近つ飛鳥の桜、滋賀・琵琶湖1泊2日旅、和歌山1泊2日旅）
- **★記事HTMLの末尾に `renderHeader('blog'); renderFooter();` の呼び出しを必ず入れる★** 忘れるとヘッダー・フッターが出ない（和歌山記事で実際に発生）
- **絵文字は使わない**（既存記事はすべて0件）
- ナビゲーション: SITE_NAVに「ブログ」追加済み（危険情報の後に配置）
- 記事追加手順: HTMLを `blog/` に配置（各スポットの節の見出しに `id="spot-{スポットID}"`）→ `blog/index.html` にカードを追加 → **`python scripts/add_blog_img_size.py` → `python generate_spot_pages.py` を流す**
- **スポットのページから記事へのリンクは自動で出る**（2026-09-25・ユーザー提案）: `generate_spot_pages.py` が `blog/*.html` を読み、本文の `spots/{id}.html` へのリンクを拾って、そのスポットのページに「このスポットに行った記事（ブログ）」を出す。対応表は持たない。記事の `<h1 class="blog-title">`・`<p class="blog-meta">`（最初の日付で新しい順）・`og:image` をそのまま使うので、**この3つの形を崩さないこと**。枠は #spotDetail の外に生HTMLで置き（クローラー用）、表示時に `js/spot.js` が「近くのスポット」の手前へ移す。記事が存在しないスポットにリンクしていると生成時に★で警告が出る
  - **★記事の中の「そのスポットの節の見出し」に `id="spot-{スポットID}"` を付ける★**（例: `<h3 id="spot-kabata-resort">`）。付いていればスポットのページのリンクがその節へ直接飛ぶ（1本の記事に複数のスポットが出るため・2026-09-25にユーザーと決定）。付いていなければ記事の先頭へ飛ぶ。見出しがヘッダーの下に隠れないよう `css/style.css` の `[id^="spot-"] { scroll-margin-top }` で余白を取っている
  - **★記事の `<img>` には width / height を必ず入れる★** `python scripts/add_blog_img_size.py` が画像ファイルの実寸を書き込む（`--check` で残りを数える）。無いと写真が読み込まれる前は高さ0で並び、節へ飛んだあとで写真が読み込まれて見出しが大きく下へずれる（2026-09-25に平草原公園の節で実際に起きた）

## テーマ別まとめページ（themes/）
- 目的・季節別に犬連れスポットを集約する固有URLのランディング群（SEO・AdSense審査でのページ厚み増を狙う）
- 配置: `themes/` 配下に静的HTML。一覧hubは `themes/index.html`
- 現在7テーマ: dogrun-free（無料ドッグラン）/ dogrun（ドッグラン全）/ sakura（桜）/ koyo（紅葉）/ water（水遊び）/ rain（雨でもOK）/ free（完全無料）
- ナビ: SITE_NAVに「まとめ」追加済み（スポット検索の次に配置、id=themes）
- **テーマ定義は `js/themes-data.js` の `THEMES` 配列で一元管理**（slug・navTitle・lead・filter）。フィルタはここが唯一の定義で、各ページ・hubが共有する
- 描画: `js/theme-page.js`（個別ページ。`window.THEME_SLUG` でテーマ指定）/ `js/themes-index.js`（hub）。一覧はspots.jsonから府県別に動的描画し件数も自動追従
- 各テーマページは**固有の導入文＋選び方のポイント**を必ず置く（薄い量産ページ化を避けるため。文体はabout.html同様のです・ます調でOK＝spots.jsonのremarks文体ルールは適用しない）
- 共通スタイルは `css/themes.css`
- **パス解決**: `/themes/` は `js/common.js` の `getBasePath()`・`resolveNavHref()` に追加済み（blog/・spots/と同様）。新サブディレクトリを増やす時は同様に両関数へ追加すること
- **新テーマ追加手順**: ①必要ならspots.jsonにtag追加 → ②`js/themes-data.js` の `THEMES` に1要素追加 → ③既存の `themes/{slug}.html` を複製して導入文・meta（title/description/canonical/og/THEME_SLUG）を差し替え → ④`generate_sitemap.py` の `theme_slugs` にslug追加 → ⑤`python generate_sitemap.py` 再生成 → commit & push
- sitemapにテーマページも含む（`generate_sitemap.py` の static_pages で生成）

## セキュリティチェック
- Desktop版は `/plugin` 非対応のため security-guidance プラグインは使えない（グローバルCLAUDE.md記載どおり）
- 実際の手段は組み込み `/security-review` スキル（AIレビュー込み・APIキー不要・動作確認済み）＋手動コード監査
- 重点チェック項目は `.claude/claude-security-guidance.md` にまとめてある（`/security-review`・手動監査時の参照リスト。`.claude/`はgitignore対象＝ローカルのみ）
- 重点: ①データ/入力をinnerHTMLに入れない（XSS）②秘密情報・PAT URLをコミットしない③自動更新JSONにHTMLタグ混入させない
- **クライアントJSでデータをinnerHTMLに挿入する時は common.js の `escapeHtml()` を必ず通す**（spots.json/dangers.json由来の name/address/remarks/description等）。URLは `^https?://` のみ許可。静的生成側(generate_spot_pages.py)は html.escape＋JSON-LDは `<>&` エスケープ済み。js全体は監査・対応済み

## 注意事項
- uchidokoroフォルダ・ファイルには絶対に触れないこと
- セッション中に汎用的な運用ルールや仕組みが決まったら、ユーザーに言われなくてもCLAUDE.mdとメモリの両方に自動で保存すること
